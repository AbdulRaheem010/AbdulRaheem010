import Image from 'next/image';import Link from 'next/link';import {notFound} from 'next/navigation';import {getProduct,products} from '@/data/products';import {site,waLink} from '@/data/site';import {JsonLd,ProductGrid} from '@/components/ui';export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getProduct(slug);return {title:p?p.name:'Product not found',description:p?.description}}export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getProduct(slug);if(!p)return notFound();const related=products.filter(x=>x.category===p.category&&x.id!==p.id).slice(0,3);return <main><JsonLd data={{'@context':'https://schema.org','@type':'Product',name:p.name,description:p.description,category:p.category,brand:site.name,offers:{'@type':'Offer',priceCurrency:'NGN',availability:'https://schema.org/InStoreOnly'}}}/><section className="section"><div className="container grid gap-10 lg:grid-cols-2"><div className="grid gap-4">{p.images.map((im,i)=><div className="relative aspect-[4/3] overflow-hidden bg-stone-100" key={im}><Image priority={i===0} src={im} alt={`${p.name} image ${i+1}`} fill className="object-cover"/></div>)}</div><div><p className="eyebrow">{p.category}</p><h1 className="serif mt-3 text-5xl font-semibold">{p.name}</h1><p className="mt-5 text-lg leading-8 text-stone-700">{p.description}</p><dl className="mt-8 grid gap-4 text-sm"><div><dt className="font-bold">Materials</dt><dd>{p.materials}</dd></div><div><dt className="font-bold">Dimensions</dt><dd>{p.dimensions}</dd></div><div><dt className="font-bold">Available colors</dt><dd>{p.colors.join(', ')}</dd></div><div><dt className="font-bold">Availability</dt><dd>{p.availability}</dd></div><div><dt className="font-bold">Delivery information</dt><dd>Delivery services are available, subject to location and order arrangements.</dd></div><div><dt className="font-bold">Price</dt><dd>{p.priceLabel}</dd></div></dl><div className="mt-8 flex flex-wrap gap-3"><a className="btn btn-primary" href={waLink(`Hello Ola Egbon Furniture Ltd., I am interested in the ${p.name}. Please provide more details and price.`)}>Enquire About This Furniture</a><a className="btn btn-secondary" href={`tel:${site.phone.replaceAll(' ','')}`}>Phone CTA</a></div></div></div></section><section className="section bg-[#f4eadc]"><div className="container"><h2 className="serif mb-8 text-4xl">Related products</h2><ProductGrid items={related}/></div></section></main>}
