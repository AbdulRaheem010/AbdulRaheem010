import {ProductGrid,SectionHeading} from '@/components/ui';import {products} from '@/data/products';import type {Metadata} from 'next';export const metadata:Metadata={title:'Shop Furniture in Ilorin',description:'Browse demo furniture catalogue items for sofas, bedroom, dining and office furniture in Ilorin.'};export default async function Shop({searchParams}:{searchParams:Promise<{category?:string}>}){const {category:cat}=await searchParams;const items=cat?products.filter(p=>p.category===cat):products;return <main className="section"><div className="container"><SectionHeading eyebrow="Shop" title="Browse Furniture" copy="Demo catalogue items make future inventory replacement simple. Please enquire for current availability and pricing."/><ProductGrid items={items}/></div></main>}
